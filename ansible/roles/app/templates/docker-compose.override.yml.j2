# Auto-generated by Ansible — replaces the weak hardcoded values in
# docker-compose.yml (docker compose merges this file automatically).
services:
  db:
    environment:
      POSTGRES_USER: "{{ postgres_user }}"
      POSTGRES_PASSWORD: "{{ postgres_password }}"
      POSTGRES_DB: "{{ postgres_db }}"
  api:
    environment:
      SECRET_KEY: "{{ secret_key }}"
